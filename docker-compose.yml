version: "3.9"

services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ./ollama:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    entrypoint: >
      bash -c "
        echo '🦙 啟動 Ollama 伺服器中...';
        ollama serve &
        sleep 8 &&
        echo '📦 檢查模型是否已存在...';
        ollama list | grep -q gemma3:4b || ollama pull gemma3:4b;
        ollama list | grep -q nomic-embed-text || ollama pull nomic-embed-text;
        echo '✅ 模型初始化完成，保持運行中...';
        tail -f /dev/null
      "
    networks:
      - chatnet   # ✅ 放進 network

  chatbot:
    build: .
    container_name: local_chatbot
    restart: always
    ports:
      - "8501:8501"
    depends_on:
      - ollama
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434   # ✅ 改掉 localhost
    volumes:
      - ./pdfFiles:/app/pdfFiles
      - ./vectorDB:/app/vectorDB
    command: >
      sh -c "streamlit run main.py --server.port=8501 --server.address=0.0.0.0"
    networks:
      - chatnet   # ✅ 放進 network

networks:
  chatnet:
    driver: bridge
